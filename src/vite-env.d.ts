/// <reference types="vite/client" />

declare module '@editorjs/link' {
    const LinkTool: any; // Use `any` type as a temporary solution, or define specific types if available
    export default LinkTool;
  }